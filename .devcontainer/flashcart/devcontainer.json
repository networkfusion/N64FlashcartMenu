{
    "name": "N64FlashcartMenu-sc64deployer",
    "build": {
        "dockerfile": "Dockerfile.sc64deployer"
    },
    "runArgs": ["--add-host=host.docker.internal:host-gateway"],
    "mounts": [
        "source=n64flashcartmenu-flashcart-bashhistory,target=/commandhistory,type=volume"
    ],
    "postCreateCommand": "git submodule update --init && cd ./libdragon && N64_INST=/opt/libdragon make clobber -j && N64_INST=/opt/libdragon make libdragon tools -j && N64_INST=/opt/libdragon make install tools-install -j",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.makefile-tools"
            ],
            "settings": {
                "git.ignoredRepositories": [
                    "libdragon",
                    "src/libs/libspng",
                    "src/libs/minimp3",
                    "src/libs/miniz"
                ]
            }
        }
    }
}
